Global language scan checkpoint.

Arabic strings that are intentional translation values remain in lang.js. UI code must use translation keys and current language selection rather than hard-coded Arabic labels.
